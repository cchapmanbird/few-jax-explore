# few-jax-explore

A small evolving library for understanding how FastEMRIWaveforms can be implemented in JAX. Of particular interest is what the potential limitations are and how these can be mitigated.

This repo houses a working `KerrEccentricEquatorial` implementation for prograde systems (if you supply the data!) which can be run in `sandbox/test-waveform.ipynb`.
